package lesson_12.ArrayMetods.homework23.model;
/*
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.CsvSource;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class RoomAssignerTest {

    TestDataGenerator.RoomAssigner assigner = new TestDataGenerator.RoomAssigner();

    @ParameterizedTest
    @CsvSource({
            "John, Critical, ICU",
            "Alice, Infectious, Isolation",
            "Bob, General, General",
            "'', General, General"
    })
    void testAssignRoom(String patientName, String condition, String expectedRoom) {
        assertEquals(expectedRoom, assigner.assignRoom(patientName, condition));
    }

} // test ended
*/
