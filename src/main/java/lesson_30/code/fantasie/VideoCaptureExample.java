package fantasie;
/*
import org.bytedeco.javacv.*;
import org.bytedeco.javacv.CanvasFrame;

    public class VideoCaptureExample {
        public static void main(String[] args) {
            FrameGrabber grabber = new OpenCVFrameGrabber(0); // 0 - нулевая камера

            try {
                grabber.start(); // Старт захвата
                CanvasFrame frame = new CanvasFrame("Video");

                while (frame.isVisible() && grabber.grab() != null) {
                    frame.showImage(grabber.grab());
                }

                grabber.stop(); // Остановка захвата
                frame.dispose(); // Закрытие окна
            } catch (FrameGrabber.Exception e) {
                e.printStackTrace();
            }
        }
    } // klass ended

*/